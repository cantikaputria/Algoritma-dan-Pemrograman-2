/* Saya Cantika Putri Arbiliansyah dengan NIM [2103727] mengerjakan 
Tugas Praktikum 8 dalam mata kuliah Algoritma dan Pemrograman 1
untuk keberkahanNya maka saya tidak melakukan kecurangan seperti 
yang telah dispesifikasikan. Aamiin.
*/

//deklarasi library
#include "header.h"

//kodingan utama
int main()
{
    scanf("%s", &str);        //meminta masukan string dari user
    scanf("%d", &n);          //meminta masukan jumlah dari user

    for (i = 0; i < n; i++)   //meminta masukan berdasarkan n
    {
        scanf(" %c %d %s", &kode, &energi, &ubah[i]);
    }
    
    scanf("%d", &m);            //meminta masukan m

    for (i = 0; i < m; i++)
    {
        scanf(" %c", &kode2);   //meminta masukan berdasarkan nilai m
    }
    
    /*
    void periksa(char kode, int energi, cahr kode2) */
    
    printf("%s", &str);
    return 0;
}