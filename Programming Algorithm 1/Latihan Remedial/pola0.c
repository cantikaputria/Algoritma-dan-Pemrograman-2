#include <stdio.h>

int main () 
{
   void angka0 (int n)
    {
        if (ascii[i] = 0)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n"); 
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    } 

    void angka1 ()
    {
        if (ascii[i] = 2)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }
        }
    }

    void angka2 ()
    {
        if (ascii[i] = 2)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    }

    void angka3 ()
    {
        if (ascii[i] = 3)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    }

    void angka4 ()
    {
        if (ascii[i] = 4)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }
        }
    }

    void angka5 ()
    {
        if (ascii[i] = 5)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    }

    void angka6 ()
    {
        if (ascii[i] = 6)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    }

    void angka7 ()
    {
        if (ascii[i] = 7)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }
        }
    }

    void angka8 ()
    {
        if (ascii[i] = 8)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    }

    void angka9 ()
    {
        if (ascii[i] = 9)
        {
            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

            for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

             for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }

             for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }
                printf("\n");
            }

             for (baris = 1; baris <= n; baris++)
            {
                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf("B");
                }

                for (kolom = 1; kolom <= n; kolom++)
                {
                    printf(" ");
                }
                printf("\n");
            }
        }
    }
    
    return 0;
}