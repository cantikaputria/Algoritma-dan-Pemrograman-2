#include <stdio.h>

int main ()
{
    int n, i;
    scanf("%d", &n);
    for (i = 1; i <= n; i++)
    {
     printf("alpro untuk masa depanku yang cemerlang %d\n", i);
    }
    
    return 0;
}