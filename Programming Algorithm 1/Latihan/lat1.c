#include <stdio.h>
#include <string.h>

int main ()
{
    char teks1[10];
    scanf("%s", &teks1);
    int length = strlen(teks1);

    char teks2[10];
    scanf("%s", &teks2);
    int length = strlen(teks2);

    char teks3[10];
    scanf("%s", &teks3);
    int length = strlen(teks3);

    char teks4[10];
    scanf("%s", &teks4);
    int length = strlen(teks4);

    char teks5[10];
    int length = strlen(teks5);








    return 0;
}