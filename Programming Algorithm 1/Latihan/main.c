/*

Saya Cantika Putri Arbiliansyah dengan NIM [2103727] mengerjakan 
soal Kuis 2 No 2 dalam mata kuliah Algoritma dan Pemrograman 1
untuk keberkahan-Nya maka saya tidak melakukan kecurangan seperti 
yang telah dispesifikasikan. Aamiin.

*/

#include "header.h"

int main ()
{
   scanf("%d", &n);

   for (int i = 0; i < n; i++) 
   {
       scanf("%s", &string[i]);    
   }

   for (int i = 0; i < n; i++)
    {
        if (strlen(string[i]) >= spasi)
        {
            spasi = strlen(string[i]); 
        }
    }
    
    void hitung (n);s
    void tampilkan (char string[n][51]);

    return 0;
}