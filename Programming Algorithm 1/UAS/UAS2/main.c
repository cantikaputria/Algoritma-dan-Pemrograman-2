/*
    Saya Cantika Putri Arbiliansyah dengan NIM [2103727] mengerjakan 
    Soal UAS Nomor 2 dalam mata kuliah Algoritma dan Pemrograman 1
    untuk keberkahan-Nya maka saya tidak melakukan kecurangan seperti 
    yang telah dispesifikasikan. Aamiin.
*/

#include "header.h"

//deklarasi library berdasarkan file header
int main ()                         //fungsi utama yang akan dijalankan
{
    int n;                          //deklarasi variabel untuk masukan user
    scanf("%d", &n);                //meminta masukan nilai n dari user

    for (i = 0; i < n; i++)         //perulangan untuk meminta nilai array tipe terstruktur dari user
    {
        scanf("%d %d %d %d", &integer[i].x, &integer[i].y,            //meminta nilai array tipe terstruktur dari masukan user
        &integer[i].z, &integer[i].a);
    }

    proses (integer);              //mengambil nilai prosedur

    return 0;                      //nilai kembali ke semula
}